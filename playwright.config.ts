import { createPacketNestConfig } from "PacketNest-agent-playwright-config/config";

export default createPacketNestConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
